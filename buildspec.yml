version: 0.2

phases:
  install: 
    runtime-versions:
      nodejs: 10.x  # Use a specific minor version if needed
  build:
    commands:
      - echo "Packages & Dependencies are getting installed"
      - npm install  # Removed the -f flag
      - echo "Packages have been installed"
      - echo "Storing Build Artifact to the Location"  # Fixed typo

artifacts:
  type: zip
  paths:
    - '*'  # Adjust this if you only want to include specific files
